<style>
:root {
  --bg: hsl(0, 0%, 13%);
  --fg: hsl(180, 100%, 97%);
  --red: hsl(357, 81%, 69%);
  --fg-dim: hsl(197, 31%, 77%);
  --blue: hsl(221, 100%, 75%);
}
html, body {
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  overflow: hidden;
  background-color: var(--bg);
  color: var(--fg);
}
body {
  display: flex;
  flex-direction: column;
}
main {
  flex-grow: 1;
  display: flex;
  flex-direction: column;
  margin: 1em;
}

code-element {
  height:100%;
}
</style>
<script type="module" src={Astro.resolve('./CodeElement.ts')}></script>
<nav>
</nav>
<!-- <script type="module">
import {importEl} from '../loader.ts';
console.log(`hello`);
const el = importEl(
  `codeEditor`, 
  `code-element`, {
});
console.log(el);
</script> -->
<main>
  <code-element>
  </code-element>
</main>
