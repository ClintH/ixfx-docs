<!-- <link rel="stylesheet" href={Astro.resolve('../../styles/demos.css')} /> -->
<script type="module" src={Astro.resolve('./stateMachine.ts')}></script>

<style>
  .sxs {
    display: flex;
  }

  .sxs>section {
    flex-grow: 1;
    margin-right: 0rem;
  }

  .sxs>section>section {
    padding: var(--padding-text);
    display: flex;
    flex-direction: column;
  }

  section.dividerRight {
    border-right: 0.2rem solid var(--bg-dim);
  }

  section.dividerTop {
    border-top: 0.2rem solid var(--bg-dim);
  }

  hr {
    border: 0;
    border-top: 0.2rem solid var(--bg-dim);
  }

  #jsonDescr {
    padding: var(--padding-text);
  
  }

  h2 {
    font-size: 90%;
    text-transform: uppercase;
    color: var(--muted-color);
  }
  #descrValidate {
    font-size: 80%;
  }
  #descrValidate.error {
    color:hsl(6,73%,50%);
  }
  #currentState {
    margin: 10px;
    font-size: 80%;
  }
  #dataStream {
    font-size: 0.8em;
    max-width: 14em;
  }
</style>
<div class="sxs">
  <section id="dataDescr" class="dividerRight">
    <section>
      <h2>Machine description</h2>
      <pre class="editable" contenteditable id="jsonDescr">
      </pre>
      <div id="descrValidate"></div>
      <p>&nbsp;</p>
      <label>
        Initial state:
        <select id="selDescrInitial">
          <option></option>
        </select>
      </label>
      <div class="toolbar">
        <button id="btnSetDescr">1. Set</button>
      </div>
    </section>
    <section>
      <label>
        Load demo:
        <select id="selDemoMachines">
          <option>one</option>
        </select>
      </label>
    </section>
  </section>
  <section >
    <section>
      <h2>Control</h2>
      <select disabled size="3" id="selPossibleNext">
        <option></option>
      </select>
      <div class="toolbar">
        <button disabled id="btnChangeState">2. Change state</button>
      </div>
   
      <div id="currentState"></div>

    </section>
    <section class="dividerTop">
      <div id="dataStream"></div>
    </section>
  </section>
  
</div>
