import { c as createComponent, r as renderTemplate, a as renderComponent, u as unescapeHTML } from './astro/server__cawITtY.mjs';
import 'kleur/colors';
import { $ as $$MainLayout } from './MainLayout_BFrOPY-O.mjs';

const html = "<p><a href=\"https://clinth.github.io/ixfx/modules/Collections.Queues.html\">API Docs: Queues</a></p>\n<p>A queue stores items like a queue at a bakery. Items added are put at the <em>back</em> of the queue, and if you want to grab an item, you can only see, and grab from the <em>front</em> of the queue. This is useful if you want to process items in the same order as they are added to the queue</p>\n<p>In other words, it is FIFO (first in, first out). If you want last-in first out, see <a href=\"../stack/\">Stack</a>.</p>\n<p>The default implementation in ixfx is immutable, meaning that every operation that changes the queue returns a <em>new</em> queue. A queue instance itself never changes.</p>\n<script type=\"module\" hoist=\"\">\nimport '/src/components/data/collections/ArrayVisElement';\nimport '/src/components/data/collections/QueueVis';\n</script>\n<div class=\"centered toolbar\">\n  <button id=\"btnEnqueue\">Enqueue</button>\n  <button id=\"btnDequeue\">Dequeue</button>\n</div>\n<div class=\"centered toolbar\">\n  <div id=\"peek\"></div>\n</div>\n<div id=\"vis\"></div>\n<h2 id=\"basics\">Basics</h2>\n<p><code>enqueue</code> and <code>dequeue</code> are the main means of interacting with a dequeue.</p>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"js\"><code><span class=\"line\"><span style=\"color:#F97583\">import</span><span style=\"color:#E1E4E8\"> {Queues} </span><span style=\"color:#F97583\">from</span><span style=\"color:#9ECBFF\"> \"https://unpkg.com/ixfx/dist/collections.js\"</span></span>\n<span class=\"line\"></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// Initialise</span></span>\n<span class=\"line\"><span style=\"color:#F97583\">let</span><span style=\"color:#E1E4E8\"> q </span><span style=\"color:#F97583\">=</span><span style=\"color:#E1E4E8\"> Queues.</span><span style=\"color:#B392F0\">immutable</span><span style=\"color:#E1E4E8\">();</span></span>\n<span class=\"line\"></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// Items are pushed in order from start, so `c`</span></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// is at the \"back\", while `a` is at the \"front\".</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">q </span><span style=\"color:#F97583\">=</span><span style=\"color:#E1E4E8\"> q.</span><span style=\"color:#B392F0\">enqueue</span><span style=\"color:#E1E4E8\">(</span><span style=\"color:#9ECBFF\">`a`</span><span style=\"color:#E1E4E8\">, </span><span style=\"color:#9ECBFF\">`b`</span><span style=\"color:#E1E4E8\">, </span><span style=\"color:#9ECBFF\">`c`</span><span style=\"color:#E1E4E8\">);</span></span>\n<span class=\"line\"></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// Yields `a`</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">q.peek;</span></span>\n<span class=\"line\"></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// Return a new queue with front-most item removed</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">q </span><span style=\"color:#F97583\">=</span><span style=\"color:#E1E4E8\"> q.</span><span style=\"color:#B392F0\">dequeue</span><span style=\"color:#E1E4E8\">();</span></span>\n<span class=\"line\"></span></code></pre>\n<h2 id=\"iterating\">Iterating</h2>\n<p>For the most part, you’re meant to just access the front-most item of a queue. If you find yourself needing to dig through a queue, it may not be the right data structure.</p>\n<p>That said, there are ways of iterating:</p>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"js\"><code><span class=\"line\"><span style=\"color:#6A737D\">// Iterate from back-to-front of queue</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">q.</span><span style=\"color:#B392F0\">forEach</span><span style=\"color:#E1E4E8\">(</span><span style=\"color:#FFAB70\">item</span><span style=\"color:#F97583\"> =></span><span style=\"color:#E1E4E8\"> {</span></span>\n<span class=\"line\"><span style=\"color:#6A737D\">  // do something with item</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">})</span></span>\n<span class=\"line\"></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// Iterate from front-to-back of queue</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">q.</span><span style=\"color:#B392F0\">forEachFromFront</span><span style=\"color:#E1E4E8\">(</span><span style=\"color:#FFAB70\">item</span><span style=\"color:#F97583\"> =></span><span style=\"color:#E1E4E8\"> {</span></span>\n<span class=\"line\"><span style=\"color:#6A737D\">  // do something with item</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">});</span></span>\n<span class=\"line\"></span></code></pre>\n<h2 id=\"additional-properties\">Additional properties</h2>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"js\"><code><span class=\"line\"><span style=\"color:#6A737D\">// True if queue is empty</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">q.isEmpty;</span></span>\n<span class=\"line\"></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// How many items in queue</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">q.</span><span style=\"color:#79B8FF\">length</span><span style=\"color:#E1E4E8\">;</span></span>\n<span class=\"line\"></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// Returns the underlying array</span></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// used by the stack. Be careful not to</span></span>\n<span class=\"line\"><span style=\"color:#6A737D\">// modify or it may break the stack semantics</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">q.data;</span></span>\n<span class=\"line\"></span></code></pre>\n<h2 id=\"capacity-limiting\">Capacity limiting</h2>\n<p>When creating the queue, it’s possible to set a capacity limit. This can be useful to avoid a <em>backlog</em> of data. For example, if you’re processing a queue of pressure-sensor data, if a burst of new data comes in you would rather process the freshest data first, you don’t really care about older things any longer.</p>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"js\"><code><span class=\"line\"><span style=\"color:#F97583\">let</span><span style=\"color:#E1E4E8\"> q </span><span style=\"color:#F97583\">=</span><span style=\"color:#E1E4E8\"> Queues.</span><span style=\"color:#B392F0\">immutable</span><span style=\"color:#E1E4E8\">({capacity: </span><span style=\"color:#79B8FF\">5</span><span style=\"color:#E1E4E8\">, discardPolicy: </span><span style=\"color:#9ECBFF\">`newer`</span><span style=\"color:#E1E4E8\">});</span></span>\n<span class=\"line\"></span></code></pre>\n<p>The <code>capacity</code> naturally sets the total number of items the queue will store. The <code>discardPolicy</code> determines how the size is maintained when the capacity limit is hit. This logic runs whenever something is <em>enqueued</em>.</p>\n<p>Values for <code>discardPolicy</code> are:</p>\n<ul>\n<li><code>older</code>: Removes existing items from the front of the queue to make room for additions. Ie. the oldest items are thrown away</li>\n<li><code>newer</code>: Removes existing items from the back of the queue to make room for additions. Ie. the newest items are thrown away</li>\n<li><code>additions</code>: Does not change the existing items, but rather throws away items that are being <em>enqueued</em>.</li>\n</ul>\n<script type=\"module\" hoist=\"\">\nimport '/src/components/data/collections/ArrayVisElement';\nimport '/src/components/data/collections/QueueDiscardVis';\n</script>\n<div class=\"centered toolbar\">\n  <button id=\"btnEnqueueDiscard\">Enqueue</button>\n  <button id=\"btnDequeueDiscard\">Dequeue</button>\n  <label for=\"selDiscard\" style=\"align-self: center\">Capacity 3.<br> Discard policy</label>\n  <select id=\"selDiscard\">\n    <option value=\"older\">older</option>\n    <option value=\"newer\">newer</option>\n    <option value=\"additions\">additions</option>\n  </select>\n</div>\n<div class=\"centered toolbar\">\n  <div id=\"peekDiscard\"></div>\n</div>\n<div id=\"visDiscard\"></div>";

				const frontmatter = {"title":"Queue","layout":"../../../layouts/MainLayout.astro"};
				const file = "/Users/af4766/repos/ixfx-docs/src/pages/data/collections/queue.md";
				const url = "/data/collections/queue";
				function rawContent() {
					return "\n[API Docs: Queues](https://clinth.github.io/ixfx/modules/Collections.Queues.html)\n\nA queue stores items like a queue at a bakery. Items added are put at the _back_ of the queue, and if you want to grab an item, you can only see, and grab from the _front_ of the queue. This is useful if you want to process items in the same order as they are added to the queue\n\nIn other words, it is FIFO (first in, first out). If you want last-in first out, see [Stack](../stack/).\n\nThe default implementation in ixfx is immutable, meaning that every operation that changes the queue returns a _new_ queue. A queue instance itself never changes.\n\n<script type=\"module\" hoist>\nimport '/src/components/data/collections/ArrayVisElement';\nimport '/src/components/data/collections/QueueVis';\n</script>\n<div class=\"centered toolbar\">\n  <button id=\"btnEnqueue\">Enqueue</button>\n  <button id=\"btnDequeue\">Dequeue</button>\n</div>\n<div class=\"centered toolbar\">\n  <div id=\"peek\"></div>\n</div>\n<div id=\"vis\"></div>\n\n\n## Basics\n\n`enqueue` and `dequeue` are the main means of interacting with a dequeue.\n\n```js\nimport {Queues} from \"https://unpkg.com/ixfx/dist/collections.js\"\n\n// Initialise\nlet q = Queues.immutable();\n\n// Items are pushed in order from start, so `c`\n// is at the \"back\", while `a` is at the \"front\".\nq = q.enqueue(`a`, `b`, `c`);\n\n// Yields `a`\nq.peek;\n\n// Return a new queue with front-most item removed\nq = q.dequeue();\n```\n\n## Iterating\n\nFor the most part, you're meant to just access the front-most item of a queue. If you find yourself needing to dig through a queue, it may not be the right data structure.\n\nThat said, there are ways of iterating:\n\n```js\n// Iterate from back-to-front of queue\nq.forEach(item => {\n  // do something with item\n})\n\n// Iterate from front-to-back of queue\nq.forEachFromFront(item => {\n  // do something with item\n});\n```\n\n## Additional properties\n\n```js\n// True if queue is empty\nq.isEmpty;\n\n// How many items in queue\nq.length;\n\n// Returns the underlying array\n// used by the stack. Be careful not to\n// modify or it may break the stack semantics\nq.data;\n```\n\n## Capacity limiting\n\nWhen creating the queue, it's possible to set a capacity limit. This can be useful to avoid a _backlog_ of data. For example, if you're processing a queue of pressure-sensor data, if a burst of new data comes in you would rather process the freshest data first, you don't really care about older things any longer.\n\n```js\nlet q = Queues.immutable({capacity: 5, discardPolicy: `newer`});\n```\n\nThe `capacity` naturally sets the total number of items the queue will store. The `discardPolicy` determines how the size is maintained when the capacity limit is hit. This logic runs whenever something is _enqueued_.\n\nValues for `discardPolicy` are:\n* `older`: Removes existing items from the front of the queue to make room for additions. Ie. the oldest items are thrown away\n* `newer`: Removes existing items from the back of the queue to make room for additions. Ie. the newest items are thrown away\n* `additions`: Does not change the existing items, but rather throws away items that are being _enqueued_. \n\n<script type=\"module\" hoist>\nimport '/src/components/data/collections/ArrayVisElement';\nimport '/src/components/data/collections/QueueDiscardVis';\n</script>\n<div class=\"centered toolbar\">\n  <button id=\"btnEnqueueDiscard\">Enqueue</button>\n  <button id=\"btnDequeueDiscard\">Dequeue</button>\n  <label for=\"selDiscard\" style=\"align-self: center\">Capacity 3.<br /> Discard policy</label>\n  <select id=\"selDiscard\">\n    <option value=\"older\">older</option>\n    <option value=\"newer\">newer</option>\n    <option value=\"additions\">additions</option>\n  </select>\n</div>\n<div class=\"centered toolbar\">\n  <div id=\"peekDiscard\"></div>\n</div>\n<div id=\"visDiscard\"></div>";
				}
				function compiledContent() {
					return html;
				}
				function getHeadings() {
					return [{"depth":2,"slug":"basics","text":"Basics"},{"depth":2,"slug":"iterating","text":"Iterating"},{"depth":2,"slug":"additional-properties","text":"Additional properties"},{"depth":2,"slug":"capacity-limiting","text":"Capacity limiting"}];
				}

				const Content = createComponent((result, _props, slots) => {
					const { layout, ...content } = frontmatter;
					content.file = file;
					content.url = url;

					return renderTemplate`${renderComponent(result, 'Layout', $$MainLayout, {
								file,
								url,
								content,
								frontmatter: content,
								headings: getHeadings(),
								rawContent,
								compiledContent,
								'server:root': true,
							}, {
								'default': () => renderTemplate`${unescapeHTML(html)}`
							})}`;
				});

export { Content, compiledContent, Content as default, file, frontmatter, getHeadings, rawContent, url };
