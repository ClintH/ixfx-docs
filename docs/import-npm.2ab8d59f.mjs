import { c as createMetadata, a as createAstro, b as createComponent, r as render, d as renderComponent } from './chunks/index.7bfc2e7e.mjs';
import { $ as $$module1, a as $$MainLayout } from './chunks/MainLayout.fae6d6b7.mjs';
import { $ as $$module2 } from './ModuleList.495e80c3.mjs';
import 'shorthash';
import 'serialize-javascript';
import 'preact/hooks';
import 'preact/jsx-runtime';
import 'lit';

const metadata = { "headers": [], "source": '\nAssuming you have Node.js and NPM installed on your machine, you can install ixfx with:\n\n```\nnpm install ixfx\n```\n\nYou\'ll then need to you use your bundler of choice to include ixfx.\n\n\nExample module import:\n\n```js\nimport { Flow } from "ixfx"\nFlow.continuously(...);\n```\n\nExample function import:\n\n```js\nimport { continuously } from "ixfx/lib/flow.js"\ncontinuously(...);\n```\n\nNote the path for modules contains `lib/` before the module file. This is different than using a URL import.', "html": `<p>Assuming you have Node.js and NPM installed on your machine, you can install ixfx with:</p>
<pre class="language-null"><code is:raw class="language-null">npm install ixfx</code></pre>
<p>You'll then need to you use your bundler of choice to include ixfx.</p>
<p>Example module import:</p>
<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span> Flow <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"ixfx"</span>
Flow<span class="token punctuation">.</span><span class="token function">continuously</span><span class="token punctuation">(</span><span class="token operator">...</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>Example function import:</p>
<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span> continuously <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"ixfx/lib/flow.js"</span>
<span class="token function">continuously</span><span class="token punctuation">(</span><span class="token operator">...</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>Note the path for modules contains <code is:raw>lib/</code> before the module file. This is different than using a URL import.</p>` };
const frontmatter = { "title": "Importing via NPM", "description": "Importing the library", "astro": { "headers": [], "source": '\nAssuming you have Node.js and NPM installed on your machine, you can install ixfx with:\n\n```\nnpm install ixfx\n```\n\nYou\'ll then need to you use your bundler of choice to include ixfx.\n\n\nExample module import:\n\n```js\nimport { Flow } from "ixfx"\nFlow.continuously(...);\n```\n\nExample function import:\n\n```js\nimport { continuously } from "ixfx/lib/flow.js"\ncontinuously(...);\n```\n\nNote the path for modules contains `lib/` before the module file. This is different than using a URL import.', "html": `<p>Assuming you have Node.js and NPM installed on your machine, you can install ixfx with:</p>
<pre class="language-null"><code is:raw class="language-null">npm install ixfx</code></pre>
<p>You'll then need to you use your bundler of choice to include ixfx.</p>
<p>Example module import:</p>
<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span> Flow <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"ixfx"</span>
Flow<span class="token punctuation">.</span><span class="token function">continuously</span><span class="token punctuation">(</span><span class="token operator">...</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>Example function import:</p>
<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span> continuously <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"ixfx/lib/flow.js"</span>
<span class="token function">continuously</span><span class="token punctuation">(</span><span class="token operator">...</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>Note the path for modules contains <code is:raw>lib/</code> before the module file. This is different than using a URL import.</p>` } };
const $$metadata = createMetadata("/src/pages/import-npm.md", { modules: [{ module: $$module1, specifier: "../layouts/MainLayout.astro", assert: {} }, { module: $$module2, specifier: "./ModuleList.astro", assert: {} }], hydratedComponents: [], clientOnlyComponents: [], hydrationDirectives: /* @__PURE__ */ new Set([]), hoisted: [] });
const $$Astro = createAstro("/src/pages/import-npm.md", "https://clinth.github.io/ixfx-docs/", "file:///Users/af4766/repos/ixfx-docs/");
const $$ImportNpm = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$ImportNpm;
  const $$content = { "title": "Importing via NPM", "description": "Importing the library", "astro": { "headers": [], "source": '\nAssuming you have Node.js and NPM installed on your machine, you can install ixfx with:\n\n```\nnpm install ixfx\n```\n\nYou\'ll then need to you use your bundler of choice to include ixfx.\n\n\nExample module import:\n\n```js\nimport { Flow } from "ixfx"\nFlow.continuously(...);\n```\n\nExample function import:\n\n```js\nimport { continuously } from "ixfx/lib/flow.js"\ncontinuously(...);\n```\n\nNote the path for modules contains `lib/` before the module file. This is different than using a URL import.', "html": `<p>Assuming you have Node.js and NPM installed on your machine, you can install ixfx with:</p>
<pre class="language-null"><code is:raw class="language-null">npm install ixfx</code></pre>
<p>You'll then need to you use your bundler of choice to include ixfx.</p>
<p>Example module import:</p>
<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span> Flow <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"ixfx"</span>
Flow<span class="token punctuation">.</span><span class="token function">continuously</span><span class="token punctuation">(</span><span class="token operator">...</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>Example function import:</p>
<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span> continuously <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"ixfx/lib/flow.js"</span>
<span class="token function">continuously</span><span class="token punctuation">(</span><span class="token operator">...</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>Note the path for modules contains <code is:raw>lib/</code> before the module file. This is different than using a URL import.</p>` } };
  return render`${renderComponent($$result, "Layout", $$MainLayout, { "content": $$content }, { "default": () => render`<p>Assuming you have Node.js and NPM installed on your machine, you can install ixfx with:</p><pre class="language-null"><code class="language-null">npm install ixfx</code></pre><p>You'll then need to you use your bundler of choice to include ixfx.</p><p>Example module import:</p><pre class="language-js"><code class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span> Flow <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"ixfx"</span>
Flow<span class="token punctuation">.</span><span class="token function">continuously</span><span class="token punctuation">(</span><span class="token operator">...</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre><p>Example function import:</p><pre class="language-js"><code class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span> continuously <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"ixfx/lib/flow.js"</span>
<span class="token function">continuously</span><span class="token punctuation">(</span><span class="token operator">...</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre><p>Note the path for modules contains <code>lib/</code> before the module file. This is different than using a URL import.</p>` })}`;
});

export { $$metadata, $$ImportNpm as default, frontmatter, metadata };
