import { c as createMetadata, a as createAstro, b as createComponent, r as render, d as renderComponent } from './chunks/index.7bfc2e7e.mjs';
import { $ as $$module1, a as $$MainLayout } from './chunks/MainLayout.fae6d6b7.mjs';
import { $ as $$module2 } from './ModuleList.495e80c3.mjs';
import 'shorthash';
import 'serialize-javascript';
import 'preact/hooks';
import 'preact/jsx-runtime';
import 'lit';

const metadata = { "headers": [{ "depth": 2, "slug": "via-url-import", "text": "Via URL import" }], "source": '\n## Via URL import\n\nYou can import the whole package with a URL to _bundle.js_\n```js\nimport {Timers} from "https://unpkg.com/ixfx/dist/bundle.js";\n\n// Use something from the module, eg:\nTimers.continuously( ... )\n```\n\nOr an individual module, note _flow.js_ instead:\n\n```js\nimport {continuously} from "https://unpkg.com/ixfx/dist/flow.js";\n\n// Use something from the module, eg:\ncontinuously( ... )\n```\n\nA starting template is available on [Glitch](https://glitch.com/edit/#!/ixfx-starter-url?path=script.js%3A1%3A0)', "html": '<h2 id="via-url-import">Via URL import</h2>\n<p>You can import the whole package with a URL to <em>bundle.js</em></p>\n<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span>Timers<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"https://unpkg.com/ixfx/dist/bundle.js"</span><span class="token punctuation">;</span>\n\n<span class="token comment">// Use something from the module, eg:</span>\nTimers<span class="token punctuation">.</span><span class="token function">continuously</span><span class="token punctuation">(</span> <span class="token operator">...</span> <span class="token punctuation">)</span></code></pre>\n<p>Or an individual module, note <em>flow.js</em> instead:</p>\n<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span>continuously<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"https://unpkg.com/ixfx/dist/flow.js"</span><span class="token punctuation">;</span>\n\n<span class="token comment">// Use something from the module, eg:</span>\n<span class="token function">continuously</span><span class="token punctuation">(</span> <span class="token operator">...</span> <span class="token punctuation">)</span></code></pre>\n<p>A starting template is available on <a href="https://glitch.com/edit/#!/ixfx-starter-url?path=script.js%3A1%3A0">Glitch</a></p>' };
const frontmatter = { "title": "Importing via URL", "astro": { "headers": [{ "depth": 2, "slug": "via-url-import", "text": "Via URL import" }], "source": '\n## Via URL import\n\nYou can import the whole package with a URL to _bundle.js_\n```js\nimport {Timers} from "https://unpkg.com/ixfx/dist/bundle.js";\n\n// Use something from the module, eg:\nTimers.continuously( ... )\n```\n\nOr an individual module, note _flow.js_ instead:\n\n```js\nimport {continuously} from "https://unpkg.com/ixfx/dist/flow.js";\n\n// Use something from the module, eg:\ncontinuously( ... )\n```\n\nA starting template is available on [Glitch](https://glitch.com/edit/#!/ixfx-starter-url?path=script.js%3A1%3A0)', "html": '<h2 id="via-url-import">Via URL import</h2>\n<p>You can import the whole package with a URL to <em>bundle.js</em></p>\n<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span>Timers<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"https://unpkg.com/ixfx/dist/bundle.js"</span><span class="token punctuation">;</span>\n\n<span class="token comment">// Use something from the module, eg:</span>\nTimers<span class="token punctuation">.</span><span class="token function">continuously</span><span class="token punctuation">(</span> <span class="token operator">...</span> <span class="token punctuation">)</span></code></pre>\n<p>Or an individual module, note <em>flow.js</em> instead:</p>\n<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span>continuously<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"https://unpkg.com/ixfx/dist/flow.js"</span><span class="token punctuation">;</span>\n\n<span class="token comment">// Use something from the module, eg:</span>\n<span class="token function">continuously</span><span class="token punctuation">(</span> <span class="token operator">...</span> <span class="token punctuation">)</span></code></pre>\n<p>A starting template is available on <a href="https://glitch.com/edit/#!/ixfx-starter-url?path=script.js%3A1%3A0">Glitch</a></p>' } };
const $$metadata = createMetadata("/src/pages/import-url.md", { modules: [{ module: $$module1, specifier: "../layouts/MainLayout.astro", assert: {} }, { module: $$module2, specifier: "./ModuleList.astro", assert: {} }], hydratedComponents: [], clientOnlyComponents: [], hydrationDirectives: /* @__PURE__ */ new Set([]), hoisted: [] });
const $$Astro = createAstro("/src/pages/import-url.md", "https://clinth.github.io/ixfx-docs/", "file:///Users/af4766/repos/ixfx-docs/");
const $$ImportUrl = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$ImportUrl;
  const $$content = { "title": "Importing via URL", "astro": { "headers": [{ "depth": 2, "slug": "via-url-import", "text": "Via URL import" }], "source": '\n## Via URL import\n\nYou can import the whole package with a URL to _bundle.js_\n```js\nimport {Timers} from "https://unpkg.com/ixfx/dist/bundle.js";\n\n// Use something from the module, eg:\nTimers.continuously( ... )\n```\n\nOr an individual module, note _flow.js_ instead:\n\n```js\nimport {continuously} from "https://unpkg.com/ixfx/dist/flow.js";\n\n// Use something from the module, eg:\ncontinuously( ... )\n```\n\nA starting template is available on [Glitch](https://glitch.com/edit/#!/ixfx-starter-url?path=script.js%3A1%3A0)', "html": '<h2 id="via-url-import">Via URL import</h2>\n<p>You can import the whole package with a URL to <em>bundle.js</em></p>\n<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span>Timers<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"https://unpkg.com/ixfx/dist/bundle.js"</span><span class="token punctuation">;</span>\n\n<span class="token comment">// Use something from the module, eg:</span>\nTimers<span class="token punctuation">.</span><span class="token function">continuously</span><span class="token punctuation">(</span> <span class="token operator">...</span> <span class="token punctuation">)</span></code></pre>\n<p>Or an individual module, note <em>flow.js</em> instead:</p>\n<pre class="language-js"><code is:raw class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span>continuously<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"https://unpkg.com/ixfx/dist/flow.js"</span><span class="token punctuation">;</span>\n\n<span class="token comment">// Use something from the module, eg:</span>\n<span class="token function">continuously</span><span class="token punctuation">(</span> <span class="token operator">...</span> <span class="token punctuation">)</span></code></pre>\n<p>A starting template is available on <a href="https://glitch.com/edit/#!/ixfx-starter-url?path=script.js%3A1%3A0">Glitch</a></p>' } };
  return render`${renderComponent($$result, "Layout", $$MainLayout, { "content": $$content }, { "default": () => render`<h2 id="via-url-import">Via URL import</h2><p>You can import the whole package with a URL to <em>bundle.js</em></p><pre class="language-js"><code class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span>Timers<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"https://unpkg.com/ixfx/dist/bundle.js"</span><span class="token punctuation">;</span>

<span class="token comment">// Use something from the module, eg:</span>
Timers<span class="token punctuation">.</span><span class="token function">continuously</span><span class="token punctuation">(</span> <span class="token operator">...</span> <span class="token punctuation">)</span></code></pre><p>Or an individual module, note <em>flow.js</em> instead:</p><pre class="language-js"><code class="language-js"><span class="token keyword">import</span> <span class="token punctuation">{</span>continuously<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"https://unpkg.com/ixfx/dist/flow.js"</span><span class="token punctuation">;</span>

<span class="token comment">// Use something from the module, eg:</span>
<span class="token function">continuously</span><span class="token punctuation">(</span> <span class="token operator">...</span> <span class="token punctuation">)</span></code></pre><p>A starting template is available on <a href="https://glitch.com/edit/#!/ixfx-starter-url?path=script.js%3A1%3A0">Glitch</a></p>` })}`;
});

export { $$metadata, $$ImportUrl as default, frontmatter, metadata };
